# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Build / packaging
dist/
build/
*.egg

# Runtime data (the Docker container mounts a host volume at /config, and
# dev runs use ./data; nothing runtime lives inside the repo itself). We do
# NOT ignore the Python package `config/` at the repo root.
data/
assets/
*.db
*.db-journal

# IDE
.vscode/
.idea/
.DS_Store

# Tooling
.ruff_cache/
.pytest_cache/
